## 

Como rodar:

```sh

//yarn eh opcional, pode ser substituido pelo proprio npm
# npm install -g react-native yarn


cd wp-react-native-app

yarn install

// inicia no emulador ou no dispositivo conectado
// Recomendo usar o Genymotion como emulador
react-native run-android

react-native start

```
